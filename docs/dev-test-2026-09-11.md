# Dev verification — 11 September 2026

The user-authorized target is `epp-test.rnids.rs:700`. Allowlisting restored
TCP/TLS access. The saved dev WHMCS configuration uses test mode, client
certificate `/var/www/html/oblakhost.pem`, and CA `/var/www/html/root.pem`.
The client certificate expires on 11 May 2031. No production registry operations
were performed.

## Verified module fixes

- Per-instance lazy EPP clients prevent reuse across credentials or test modes.
- Dev hello and missing-client-transaction-ID compatibility match the registry;
  production retains strict settings, and present mismatched IDs always fail.
- Domain/period validation and complete contact validation precede registry work.
- Contact changes preserve existing objects, address line 2, and explicit role
  scope; malformed or unsupported role submissions cannot fall back to changing
  every role from the WHMCS account profile.
- Unicode and Punycode domain inputs normalize consistently. Nameserver updates
  use the SDK XML builders, including ASCII IDNA and IPv6 address tags.
- Pending transfers remain incomplete; registry-imposed locks cannot report a
  successful unlock.
- Logs and user errors suppress credentials, raw XML and TLS internals while
  retaining safe registry codes and transaction identifiers.
- Release metadata includes `rnids.additionalfields.php`.

## Offline and connection checks

Against the locked SDK, DDEV PHP 8.1.34 passed 239 module contracts, 35 validation
checks, 16 diagnostics checks, 7 actual-SDK nameserver XML checks, and 3 contact
event checks (300 total). These are
standalone module scripts, with no registry calls. All 21 changed/new PHP files
also passed PHP 8.1 syntax checks.

The dev preflight passed all eleven checks: test mode, endpoint, configured
credentials, readable certificates, certificate/private-key match, validity,
CA chain, TCP, the actual WHMCS configuration-validation handler, EPP login
(`1000`), and logout.

## Live findings

The first actual WHMCS handler run passed fixture preconditions but exposed an
SDK availability parser bug before registration: RNIDS returns `avail="true"`,
which the parser incorrectly treated as unavailable because it recognized only
`"1"`. A read-only wire-attribute capture confirmed this; no objects were created
by that failed run. The SDK correction was installed and the final handler run below
passed its availability assertions.

The final WHMCS run (`whmcs-260911210115-8d89e24a`) completed with **52 passed,
zero failed, and 18 pending checks**, exiting `3` (`passed-with-approval-pending`).
It verified `.rs`, company `.co.rs`, and Cyrillic `.срб` registration,
availability before/after, domain/contact/nameserver reads, Sync/TransferSync,
lock/unlock, Admin/Tech contact replacement, unchanged-contact saves, and
one-year renewal. The `.rs` child-host flow passed register retries, conflicting
glue rejection, replacement of a multi-address set, nameserver assignment and
restoration, and delete retries.

Three registrant requests were accepted with `pendingUpdate`; the old contacts
remained unchanged and the three new target contacts were verified and retained.
Approval completion is **not** claimed. Domain deletion was accepted for all
three domains but remains pending. Nine assigned contacts remain linked, and the
child host is confirmed absent. The final run has no unknown orphan handles.
The private ledger is `/tmp/rnids-whmcs-dev-20260911-final-3.json` on both the host
and DDEV container. An earlier run's unobserved pending registrant targets remain
tracked separately in `rnids-23h`.

The SDK offline gate passed **395 tests, 1,094 assertions**, PHPStan and PHPCS.
The final availability fix accepts all XML boolean forms and rejects malformed
values. The earlier 92.40% coverage result belongs specifically to SDK revision
`a93cbe1`; it is not a freshly measured coverage claim for subsequent commits.

The **complete SDK integration suite** was run at final revision
`421256a7d019db47d9517415fd05aad83897a6ee`: 6 cases, 52 assertions, **5 passed,
zero failures, 1 skipped**. The skipped case is poll acknowledgement because no
specific queue message was approved. With `--fail-on-skipped`, the command exits
`1`; this is not an all-green full-suite claim. Stable domain info used a recorded
owned test domain. Deleted contacts were confirmed absent with `info` result
`2303`; RNIDS reserves their IDs, so availability is not a deletion proof.
The private SDK report and resource ledger are under
`/tmp/rnids-sdk-verification.PYNByl/final-full-live-20260911.json` and
`final-full-live-resources-20260911.json` respectively.

## Integration package

Composer is locked to SDK `421256a7d019db47d9517415fd05aad83897a6ee` on the
review branch. Its runtime source is identical to `f2dcb3c`, which was installed
during the final WHMCS lifecycle; the subsequent changes corrected the SDK live
cleanup assertion and excluded development artifacts from source distributions.

The isolated `--no-dev --classmap-authoritative` build passed. The archive has
233 entries, includes `rnids.additionalfields.php` and the SDK IDNA encoder,
and excludes tests, agent files, beads hooks, certificates and private keys.
The authoritative autoloader was checked on PHP 8.1.34.

Artifact: `/tmp/rnids-whmcs-integration-20260911.zip`.
SHA-256: `2d5ec9e040c240d10e3a9d43c8f7516dad3f00355405c86469f9e4b3ac6085a5`.

## Remaining integration scope and cleanup

RNIDS accepts domain deletion while retaining `pendingDelete`/`pendingUpdate`.
Contacts can remain linked with result `2305` until registry processing completes.
The private live ledgers retain owned resource identifiers; pending is not
confirmed cleanup. SDK retained fixtures are tracked in `rnids-epp-client-23d`; module fixtures
are tracked in `rnids-23h`. Final UI/approval checks are tracked in `rnids-5yr`.

Full cross-registrar transfer needs a second registrar fixture. Poll acknowledgement
needs a specifically approved message. WHMCS UI ordering/billing, transfer hooks,
and notification delivery remain final application integration checks. The CLI
runner excludes GetEPPCode/IRTP notifications and does not send those messages.

See [testing.md](testing.md) for the exact runnable scripts, their endpoint guards,
exit codes, and cleanup reporting. Both repositories use the review branch
`fix/dev-integration-readiness`; no production merge or release is implied.
