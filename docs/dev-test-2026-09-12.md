# Company registration verification — 12 September 2026

The dev WHMCS `.co.rs` Auto Registration setting and the failed pending order
already select `rnids`. The registration log shows local failures for missing
company identifiers before any registry registration.

Registration now copies the registrant's resolved tax number / VAT ID into an
empty company number. Explicit company numbers and mapped `reg_mb` values retain
precedence. Empty tax aliases no longer hide a populated VAT alias. No identifier
length, format, checksum, or accuracy validation is performed; the registrant is
responsible for the information.

A guarded dry run used WHMCS `RegRegisterDomain` for the pending order and stopped
at `PreRegistrarRegisterDomain`, with EPP client construction disabled as a second
guard. It confirmed WHMCS supplies `companyname` and `tax_id`, and the corrected
contact payload contains matching company number and VAT ID. No registration or
completion email was submitted. The pending order still needs a registration retry.

The optional domain-field definitions now load without an undefined variable,
default company-only TLDs to Company, and allow blank identifier overrides for
the VAT fallback. Those optional definitions remain disabled in this dev WHMCS;
the account's registration contact data supplies the identifiers.

DDEV PHP 8.1 verification passed 250 module contracts, 35 validation checks,
16 diagnostics checks, 7 SDK nameserver XML checks, and 3 contact-event checks:
**311 passed, zero failed**. All four changed PHP files passed syntax checks.
Focused regressions were observed failing before the fixes. Independent review
found no blocking issues.

The updated integration archive is `/tmp/rnids-whmcs-integration-20260912.zip`
(233 entries), SHA-256
`d93de69875d4418dcb7b76375f4ac5538d5f1a2679acf60dc467b300a0e0ad16`.
It uses the same locked SDK revision `421256a7d019db47d9517415fd05aad83897a6ee`
and verified dependency-only vendor tree as the previous integration build.
Changed packaged files were compared byte-for-byte with the tested sources;
certificates, private keys, test scripts and agent artifacts are excluded.

Implementation is tracked in `rnids-e7d`; final WHMCS UI and registry approval
testing remains tracked in `rnids-5yr`.

## SDK 2.0.1 and IRTP fixture follow-up

The minimum SDK requirement is now `^2.0.1`, with the published v2.0.1 locked at
`3b9bac76c823227a8602d844dcca43574499ec04`. The temporary integration branch pin
and VCS repository override have been removed.

SDK 2.0.1 requires a nonempty contact phone number. The module now checks that
field before any registry access during registration and contact replacement,
returning a safe, specific missing-phone error. No phone-format or identifier
accuracy checks were added. Five new regressions failed before the fix and passed
afterward. Final DDEV PHP 8.1 results: **316 passed, zero failed** across the five
module scripts; all four changed PHP files passed lint. Composer validation passed
with only the existing missing-license warning.

All eleven dev preflight checks passed against v2.0.1. A guarded WHMCS registration
dry run also passed the actual contact payload through the SDK request factory,
without sending registration. Read-only queries for recent WHMCS domain IDs
3550, 3551 and 3552 returned Active domains with `pendingUpdate`, IRTP enabled,
pending contact verification, no pending suspension, and verification deadlines
on 2 October 2026. These flags satisfy the Lagom Contact Information banner
condition. Browser rendering and approval completion remain manual checks.

The refreshed archive is
`/tmp/rnids-whmcs-integration-sdk-2.0.1-20260912.zip` (258 entries), SHA-256
`897f6ec2360912ae0263558255bb0c665bf2f6bc8f21917abc41ac2c19203b04`.
It uses the installed v2.0.1 dependency-only vendor tree; changed packaged sources
match the tested files, and development artifacts and key material are excluded.
This follow-up is tracked in `rnids-3q8`.
